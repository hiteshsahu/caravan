# Fake GPUs for scheduling tests: count-only, no physical devices and no File=.
# scontrol reports Gres=gpu:4 / GresUsed=gpu:N without IDX ranges.
NodeName=c1 Name=gpu Count=4
NodeName=c2 Name=gpu Count=4
