ClusterName=caravan
SlurmctldHost=slurmctld

SlurmUser=root
AuthType=auth/munge

SlurmctldPort=6817
SlurmdPort=6818

StateSaveLocation=/var/spool/slurmctld
SlurmdSpoolDir=/var/spool/slurmd
SlurmctldPidFile=/run/slurmctld.pid
SlurmdPidFile=/run/slurmd.pid

ProctrackType=proctrack/linuxproc
TaskPlugin=task/none
ReturnToService=2

SchedulerType=sched/backfill
SelectType=select/cons_tres
SelectTypeParameters=CR_Core

GresTypes=gpu

AccountingStorageType=accounting_storage/none
JobAcctGatherType=jobacct_gather/none

SlurmctldLogFile=/var/log/slurm/slurmctld.log
SlurmdLogFile=/var/log/slurm/slurmd.log
SlurmctldDebug=info
SlurmdDebug=info

# CARAVAN_GPU=real variant (see docker-compose.gpu.yml): c1 gets the host's
# one real GPU (Gres=gpu:1, detected via NVML — see gres.gpu.conf); c2 has
# no device passthrough so it stays fake/count-only like the default.
NodeName=c1 NodeAddr=c1 CPUs=7 RealMemory=1941 Gres=gpu:1 State=UNKNOWN
NodeName=c2 NodeAddr=c2 CPUs=7 RealMemory=1941 Gres=gpu:fake:4 State=UNKNOWN
PartitionName=gpu Nodes=c1,c2 Default=YES MaxTime=INFINITE State=UP
