# Containers have no systemd/dbus, so the cgroup/v2 plugin can't create a
# systemd scope for each step's cgroup — IgnoreSystemd makes it manage
# cgroupfs directly instead, which is what these containers can actually do.
CgroupPlugin=cgroup/v2
IgnoreSystemd=yes
